import { Meteor } from 'meteor/meteor';
import { UserResults } from '../imports/api/userResults/userResults.js';
import from '../imports/api/userResults/methods.js';
import from '../imports/api/userResults/server/publications.js';

Meteor.startup(() => {
  // code to run on server at startup
});
